# Configuración ejemplo: Múltiples recursos
# Demuestra cómo crear varios topics y subscripciones

project: gnp-ods-qa

resources:
  # ============================================================================
  # TOPICS
  # ============================================================================
  
  - type: topic
    name: gnp-tesoreria.stela.cuentas.liquidada
  
  
  # ============================================================================
  # SUBSCRIPTIONS
  # ============================================================================
  
  - type: subscription
    name: gnp-tesoreria.stela.cuentas.liquidada.ods.recibos
    topic: gnp-tesoreria.stela.cuentas.liquidada
    ack_deadline: 600
    expiration_policy_ttl: ""